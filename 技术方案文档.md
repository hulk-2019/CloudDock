# CloudDock - 云空间存储 Chrome 插件技术方案

> 让云存储像 Docker 一样随叫随到！⚡️

## 📋 项目概述

CloudDock 是一款浏览器端的云存储管理插件，通过侧边抽屉的形式为用户提供便捷的云端文件管理能力，支持多云厂商接入、拖拽上传、截图粘贴等现代化交互方式。

**项目定位**：轻量级云存储客户端，浏览器原生集成，随时随地管理你的云端文件。

---

## 🎯 核心功能

### 1. 配置管理模块
- **云厂商选择**：支持阿里云 OSS、腾讯云 COS、七牛云、AWS S3 等主流云存储
- **AK/SK 配置**：安全存储认证信息（使用 Chrome Storage API 加密存储）
- **Bucket 配置**：支持多空间配置与快速切换

### 2. 侧边抽屉模块
- **文件浏览**：树形/列表双模式展示文件结构
- **空间切换**：下拉快速切换不同 Bucket
- **目录导航**：面包屑导航 + 快速跳转
- **多种上传方式**：
  - 📂 拖拽本地文件上传
  - 🖼️ 拖拽网页图片/视频上传
  - 📸 截图粘贴上传（Ctrl+V）
- **文件操作**：重命名、删除、移动、复制链接

### 3. 悬浮按钮模块
- **可拖拽定位**：用户自定义按钮位置
- **快捷菜单**：
  - 唤起/关闭抽屉
  - 快速设置
  - 截图上传
  - 最近文件

### 4. 帮助文档模块
- **使用手册**：插件功能介绍、操作指南
- **AK 申请教程**：各云厂商 AccessKey 申请步骤
- **FAQ**：常见问题解答
- **更新日志**：版本迭代记录

---

## 🏗️ 技术架构

### 技术栈选型

```
前端框架：React 18 + TypeScript
UI 组件库：Ant Design / Shadcn UI
状态管理：Zustand / Jotai
构建工具：Vite + CRXJS
CSS 方案：TailwindCSS
图标库：Lucide Icons / Ant Design Icons
```

### Chrome Extension 架构

```
CloudDock/
├── manifest.json          # Extension 配置文件（Manifest V3）
├── src/
│   ├── background/        # Service Worker（后台脚本）
│   │   ├── index.ts
│   │   └── storage.ts     # 配置存储管理
│   ├── content/           # Content Script（注入页面）
│   │   ├── index.tsx
│   │   ├── FloatingButton.tsx  # 悬浮按钮
│   │   └── DrawerPanel.tsx     # 侧边抽屉
│   ├── popup/             # Popup 页面（点击插件图标）
│   │   └── index.tsx
│   ├── options/           # 配置页面
│   │   └── index.tsx
│   ├── components/        # 共享组件
│   │   ├── FileList/
│   │   ├── Uploader/
│   │   ├── Breadcrumb/
│   │   └── CloudProviderSelector/
│   ├── services/          # 云存储服务层
│   │   ├── base.ts        # 基础接口定义
│   │   ├── alioss.ts      # 阿里云 OSS SDK 封装
│   │   ├── tencentcos.ts  # 腾讯云 COS SDK 封装
│   │   ├── qiniu.ts       # 七牛云 SDK 封装
│   │   └── aws-s3.ts      # AWS S3 SDK 封装
│   ├── hooks/             # React Hooks
│   │   ├── useCloudStorage.ts
│   │   ├── useFileUpload.ts
│   │   └── useDragDrop.ts
│   ├── store/             # 状态管理
│   │   ├── config.ts
│   │   ├── files.ts
│   │   └── ui.ts
│   └── utils/             # 工具函数
│       ├── crypto.ts      # AK/SK 加密
│       ├── screenshot.ts  # 截图处理
│       └── file.ts        # 文件处理
├── public/
│   ├── icons/
│   └── docs/              # 帮助文档
└── package.json
```

---

## 🔧 核心技术实现

### 1. Manifest V3 配置

```json
{
  "manifest_version": 3,
  "name": "CloudDock",
  "version": "1.0.0",
  "description": "云端存储，随手可得",
  "permissions": [
    "storage",
    "activeTab",
    "clipboardRead"
  ],
  "host_permissions": [
    "https://*.aliyuncs.com/*",
    "https://*.myqcloud.com/*",
    "https://*.qiniucdn.com/*",
    "https://*.amazonaws.com/*"
  ],
  "background": {
    "service_worker": "src/background/index.ts"
  },
  "content_scripts": [
    {
      "matches": ["<all_urls>"],
      "js": ["src/content/index.tsx"],
      "css": ["src/content/styles.css"]
    }
  ],
  "action": {
    "default_popup": "src/popup/index.html"
  },
  "options_page": "src/options/index.html"
}
```

### 2. 云存储服务抽象层

```typescript
// services/base.ts
export interface ICloudStorageProvider {
  // 初始化
  init(config: CloudConfig): Promise<void>;
  
  // 文件列表
  listFiles(path: string, bucket: string): Promise<FileItem[]>;
  
  // 上传文件
  uploadFile(file: File, path: string, bucket: string): Promise<UploadResult>;
  
  // 删除文件
  deleteFile(path: string, bucket: string): Promise<void>;
  
  // 移动文件
  moveFile(from: string, to: string, bucket: string): Promise<void>;
  
  // 获取文件链接
  getFileUrl(path: string, bucket: string): Promise<string>;
  
  // 获取 Bucket 列表
  listBuckets(): Promise<BucketInfo[]>;
}

// 工厂模式创建不同云厂商实例
export class CloudStorageFactory {
  static create(provider: CloudProvider): ICloudStorageProvider {
    switch (provider) {
      case 'aliyun':
        return new AliOSSService();
      case 'tencent':
        return new TencentCOSService();
      case 'qiniu':
        return new QiniuService();
      case 'aws':
        return new AWSS3Service();
      default:
        throw new Error('Unsupported provider');
    }
  }
}
```

### 3. 侧边抽屉实现

```typescript
// content/DrawerPanel.tsx
import { Drawer, Tree, Upload, Breadcrumb, Spin } from 'antd';
import { useCloudStorage } from '@/hooks/useCloudStorage';
import { useFileUpload } from '@/hooks/useFileUpload';

export const DrawerPanel: React.FC = () => {
  const { files, currentPath, loading, navigate, refresh } = useCloudStorage();
  const { uploadFile, uploadProgress } = useFileUpload();
  
  // 拖拽上传
  const handleDrop = async (e: React.DragEvent) => {
    e.preventDefault();
    const items = Array.from(e.dataTransfer.items);
    
    for (const item of items) {
      if (item.kind === 'file') {
        const file = item.getAsFile();
        if (file) await uploadFile(file, currentPath);
      } else if (item.kind === 'string' && item.type.includes('text/html')) {
        // 处理网页图片/视频拖拽
        const html = await new Promise<string>(resolve => item.getAsString(resolve));
        await handleMediaFromHTML(html);
      }
    }
    refresh();
  };
  
  // 截图粘贴
  const handlePaste = async (e: ClipboardEvent) => {
    const items = e.clipboardData?.items;
    if (!items) return;
    
    for (const item of Array.from(items)) {
      if (item.type.indexOf('image') !== -1) {
        const blob = item.getAsFile();
        if (blob) {
          const filename = `screenshot_${Date.now()}.png`;
          await uploadFile(new File([blob], filename), currentPath);
          refresh();
        }
      }
    }
  };
  
  return (
    <div 
      className="drawer-container"
      onDrop={handleDrop}
      onDragOver={e => e.preventDefault()}
      onPaste={handlePaste}
      tabIndex={0}
    >
      {/* 面包屑导航 */}
      <Breadcrumb items={pathToBreadcrumb(currentPath)} />
      
      {/* 文件列表 */}
      <FileList files={files} onNavigate={navigate} />
      
      {/* 上传进度 */}
      {uploadProgress > 0 && <Progress percent={uploadProgress} />}
    </div>
  );
};
```

### 4. 悬浮按钮实现

```typescript
// content/FloatingButton.tsx
import { useState, useRef } from 'react';
import { Menu, Dropdown } from 'antd';
import Draggable from 'react-draggable';

export const FloatingButton: React.FC = () => {
  const [position, setPosition] = useState({ x: 0, y: 0 });
  const [drawerVisible, setDrawerVisible] = useState(false);
  
  const menuItems = [
    { key: 'toggle', label: '打开云盘', icon: <FolderIcon /> },
    { key: 'screenshot', label: '截图上传', icon: <CameraIcon /> },
    { key: 'settings', label: '设置', icon: <SettingsIcon /> },
  ];
  
  const handleMenuClick = ({ key }: { key: string }) => {
    switch (key) {
      case 'toggle':
        setDrawerVisible(!drawerVisible);
        break;
      case 'screenshot':
        captureScreenshot();
        break;
      case 'settings':
        chrome.runtime.openOptionsPage();
        break;
    }
  };
  
  return (
    <>
      <Draggable
        position={position}
        onStop={(e, data) => {
          setPosition({ x: data.x, y: data.y });
          // 保存位置到 storage
          chrome.storage.local.set({ floatingButtonPosition: { x: data.x, y: data.y } });
        }}
      >
        <div className="floating-button">
          <Dropdown menu={{ items: menuItems, onClick: handleMenuClick }}>
            <button className="cloud-icon-btn">
              <CloudIcon />
            </button>
          </Dropdown>
        </div>
      </Draggable>
      
      <DrawerPanel visible={drawerVisible} onClose={() => setDrawerVisible(false)} />
    </>
  );
};
```

### 5. 配置存储（加密方案）

```typescript
// background/storage.ts
import CryptoJS from 'crypto-js';

const ENCRYPTION_KEY = 'clouddock-secret-key-v1'; // 实际应用中应使用更安全的密钥管理

export class SecureStorage {
  // 加密存储 AK/SK
  static async setCredentials(provider: string, ak: string, sk: string) {
    const encrypted = {
      ak: CryptoJS.AES.encrypt(ak, ENCRYPTION_KEY).toString(),
      sk: CryptoJS.AES.encrypt(sk, ENCRYPTION_KEY).toString(),
    };
    
    await chrome.storage.local.set({
      [`credentials_${provider}`]: encrypted
    });
  }
  
  // 解密读取 AK/SK
  static async getCredentials(provider: string): Promise<{ ak: string; sk: string } | null> {
    const result = await chrome.storage.local.get(`credentials_${provider}`);
    const encrypted = result[`credentials_${provider}`];
    
    if (!encrypted) return null;
    
    return {
      ak: CryptoJS.AES.decrypt(encrypted.ak, ENCRYPTION_KEY).toString(CryptoJS.enc.Utf8),
      sk: CryptoJS.AES.decrypt(encrypted.sk, ENCRYPTION_KEY).toString(CryptoJS.enc.Utf8),
    };
  }
}
```

### 6. 截图粘贴上传

```typescript
// utils/screenshot.ts
export async function captureScreenshot(): Promise<File> {
  // 使用 Chrome API 截取当前标签页
  const dataUrl = await chrome.tabs.captureVisibleTab();
  
  // 转换为 Blob
  const response = await fetch(dataUrl);
  const blob = await response.blob();
  
  // 创建 File 对象
  const filename = `screenshot_${new Date().toISOString()}.png`;
  return new File([blob], filename, { type: 'image/png' });
}

// 监听快捷键（在 content script 中）
document.addEventListener('keydown', async (e) => {
  if ((e.ctrlKey || e.metaKey) && e.shiftKey && e.key === 'U') {
    e.preventDefault();
    const screenshot = await captureScreenshot();
    await uploadFile(screenshot);
    showNotification('截图已上传到云端！');
  }
});
```

### 7. 文件移动功能

```typescript
// hooks/useFileOperations.ts
export function useFileOperations() {
  const cloudService = useCloudService();
  
  const moveFile = async (
    sourcePath: string,
    targetPath: string,
    bucket: string
  ) => {
    try {
      // 1. 复制文件到新位置
      await cloudService.copyFile(sourcePath, targetPath, bucket);
      
      // 2. 删除原文件
      await cloudService.deleteFile(sourcePath, bucket);
      
      // 3. 刷新文件列表
      await refresh();
      
      message.success('文件移动成功！');
    } catch (error) {
      message.error('文件移动失败：' + error.message);
    }
  };
  
  return { moveFile };
}

// 在 UI 中实现拖拽移动
<Tree
  draggable
  onDrop={(info) => {
    const sourceNode = info.dragNode;
    const targetNode = info.node;
    
    if (targetNode.isLeaf) return; // 只能移动到文件夹
    
    moveFile(sourceNode.key, targetNode.key, currentBucket);
  }}
/>
```

---

## 🔐 安全性设计

### 1. 敏感信息保护
- **AK/SK 加密存储**：使用 AES-256 加密，密钥存储在 Service Worker 内存中
- **HTTPS 通信**：所有云端请求强制 HTTPS
- **权限最小化**：只申请必要的 Chrome 权限

### 2. CSP（内容安全策略）
```json
{
  "content_security_policy": {
    "extension_pages": "script-src 'self'; object-src 'self'"
  }
}
```

### 3. 跨域处理
通过 `host_permissions` 声明云厂商域名白名单，避免任意跨域请求。

---

## 📦 云厂商 SDK 集成

### 支持的云厂商

| 云厂商 | SDK 包名 | 文档链接 |
|--------|---------|---------|
| 阿里云 OSS | `ali-oss` | https://help.aliyun.com/document_detail/64041.html |
| 腾讯云 COS | `cos-js-sdk-v5` | https://cloud.tencent.com/document/product/436/11459 |
| 七牛云 | `qiniu-js` | https://developer.qiniu.com/kodo/sdk/1283/javascript |
| AWS S3 | `@aws-sdk/client-s3` | https://docs.aws.amazon.com/sdk-for-javascript/v3 |

### 示例：阿里云 OSS 集成

```typescript
// services/alioss.ts
import OSS from 'ali-oss';
import { ICloudStorageProvider } from './base';

export class AliOSSService implements ICloudStorageProvider {
  private client: OSS | null = null;
  
  async init(config: CloudConfig): Promise<void> {
    this.client = new OSS({
      region: config.region,
      accessKeyId: config.accessKeyId,
      accessKeySecret: config.accessKeySecret,
      bucket: config.bucket,
    });
  }
  
  async listFiles(path: string, bucket: string): Promise<FileItem[]> {
    const result = await this.client!.list({
      prefix: path,
      delimiter: '/',
    });
    
    return result.objects.map(obj => ({
      name: obj.name,
      size: obj.size,
      type: obj.name.endsWith('/') ? 'folder' : 'file',
      lastModified: obj.lastModified,
      url: obj.url,
    }));
  }
  
  async uploadFile(file: File, path: string, bucket: string): Promise<UploadResult> {
    const fullPath = `${path}/${file.name}`;
    const result = await this.client!.put(fullPath, file);
    
    return {
      url: result.url,
      name: file.name,
      size: file.size,
    };
  }
  
  // ... 其他方法实现
}
```

---

## 🎨 UI/UX 设计要点

### 抽屉设计
- **宽度**：400px（可调整）
- **位置**：页面右侧滑出
- **遮罩**：半透明黑色背景，点击关闭
- **动画**：300ms 滑动动画

### 悬浮按钮设计
- **尺寸**：56x56px 圆形按钮
- **默认位置**：右下角，距离边缘 24px
- **拖拽反馈**：拖动时放大 1.1 倍，添加阴影
- **吸附效果**：靠近边缘时自动吸附

### 文件列表设计
- **视图模式**：列表视图（默认）/ 网格视图
- **图标**：根据文件类型显示不同图标
- **排序**：按名称、大小、修改时间排序
- **搜索**：支持文件名模糊搜索

### 上传进度提示
- **整体进度条**：顶部显示总体上传进度
- **单文件进度**：每个文件显示独立进度条
- **成功提示**：Toast 通知 + 绿色勾选动画

---

## 🚀 开发计划

### 第一阶段：核心功能（2周）
- [x] 项目脚手架搭建
- [ ] Manifest V3 配置
- [ ] 基础 UI 框架（抽屉、悬浮按钮）
- [ ] 配置页面（云厂商选择、AK/SK 设置）
- [ ] 阿里云 OSS 集成
- [ ] 文件列表展示

### 第二阶段：上传功能（1.5周）
- [ ] 拖拽文件上传
- [ ] 拖拽网页图片/视频上传
- [ ] 截图粘贴上传
- [ ] 上传进度显示
- [ ] 批量上传队列管理

### 第三阶段：文件管理（1周）
- [ ] 文件重命名
- [ ] 文件删除
- [ ] 文件移动（拖拽）
- [ ] 复制文件链接
- [ ] 面包屑导航

### 第四阶段：多云支持（1周）
- [ ] 腾讯云 COS 集成
- [ ] 七牛云集成
- [ ] AWS S3 集成
- [ ] 云厂商快速切换

### 第五阶段：优化与文档（0.5周）
- [ ] 性能优化（虚拟滚动、懒加载）
- [ ] 错误处理与日志
- [ ] 帮助文档编写
- [ ] 使用手册录制

---

## 📊 性能优化策略

### 1. 文件列表优化
- **虚拟滚动**：使用 `react-window` 渲染大量文件
- **分页加载**：每次加载 100 条，滚动到底部自动加载
- **缓存策略**：文件列表缓存 5 分钟

### 2. 上传优化
- **分片上传**：大文件（>100MB）自动分片
- **并发控制**：最多同时上传 3 个文件
- **断点续传**：支持上传失败后继续

### 3. 内存管理
- **图片预览**：使用 `createObjectURL` 而非 Base64
- **及时释放**：文件上传后立即释放内存引用

---

## 🧪 测试策略

### 单元测试
- 使用 Vitest + Testing Library
- 覆盖核心业务逻辑（上传、下载、文件操作）
- 目标覆盖率：80%+

### E2E 测试
- 使用 Puppeteer + Chrome Extension Testing Library
- 测试完整用户流程：配置 → 上传 → 管理

### 手动测试清单
- [ ] 各云厂商连接测试
- [ ] 拖拽上传（本地文件、网页图片）
- [ ] 截图粘贴上传
- [ ] 文件移动、删除
- [ ] 悬浮按钮拖拽
- [ ] 面包屑导航
- [ ] 权限配置错误提示

---

## 📚 帮助文档规划

### 1. 使用手册
- **快速开始**：5 分钟上手指南
- **功能介绍**：每个功能模块的详细说明
- **常见场景**：截图上传、网页素材收集等
- **快捷键列表**：Ctrl+Shift+U 截图上传等

### 2. AK 申请教程
- **阿里云 OSS**：
  1. 登录阿里云控制台
  2. 访问 RAM 访问控制
  3. 创建 AccessKey
  4. 授予 OSS 权限
  5. 复制 AK/SK 到插件配置

- **腾讯云 COS**：类似步骤
- **七牛云**：类似步骤
- **AWS S3**：类似步骤

### 3. FAQ
- Q: AK/SK 安全吗？
  - A: 插件使用 AES-256 加密存储，只保存在本地浏览器中。

- Q: 支持哪些文件格式？
  - A: 支持所有格式，没有限制。

- Q: 上传大小有限制吗？
  - A: 取决于你的云存储配置，插件本身无限制。

---

## 🔄 迭代规划

### V1.0（MVP）
- 支持阿里云 OSS
- 基础文件浏览、上传、删除
- 拖拽上传、截图粘贴

### V1.1
- 支持腾讯云 COS、七牛云
- 文件移动、重命名
- 批量操作

### V1.2
- 支持 AWS S3
- 文件搜索
- 视图模式切换（列表/网格）

### V2.0
- 离线缓存
- 文件同步
- 团队协作功能

---

## 💰 成本与资源评估

### 开发资源
- **前端开发**：1 人 × 6 周
- **测试**：1 人 × 1 周
- **设计**：1 人 × 2 周（兼职）

### 云服务成本
- **存储费用**：由用户自行承担（使用自己的云账号）
- **CDN 流量**：无需插件方承担

### 技术债务控制
- 代码规范：ESLint + Prettier
- 类型检查：TypeScript strict 模式
- 代码审查：必须 Review 通过才能合并

---

## 📝 总结

CloudDock 是一个技术栈现代、架构清晰的 Chrome 插件项目。通过抽象云存储服务层，可以方便地扩展支持更多云厂商。采用 Manifest V3 确保了长期兼容性，加密存储保证了用户凭证安全。

**核心亮点**：
1. 🎯 多云统一管理，一个插件搞定所有云存储
2. 🚀 现代化交互，拖拽、截图粘贴，丝滑流畅
3. 🔐 安全可靠，AK/SK 加密存储，数据不离开本地
4. 🎨 界面优雅，抽屉式设计不干扰浏览体验
5. 🔧 易于扩展，插件化架构，支持更多云厂商

---

**文档版本**：v1.0  
**最后更新**：2026/08/06  
**作者**：CloudDock Team  

---

## 附录

### 参考资料
- [Chrome Extension 官方文档](https://developer.chrome.com/docs/extensions/mv3/)
- [阿里云 OSS JavaScript SDK](https://help.aliyun.com/document_detail/64041.html)
- [腾讯云 COS JavaScript SDK](https://cloud.tencent.com/document/product/436/11459)
- [React 官方文档](https://react.dev/)
- [TypeScript 官方文档](https://www.typescriptlang.org/)

### 开源许可
建议采用 MIT License，便于商业化和社区贡献。
